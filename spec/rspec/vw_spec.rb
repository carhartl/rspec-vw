require 'spec_helper'

describe RSpec::Vw do
  it 'has a version number' do
    expect(RSpec::Vw::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
